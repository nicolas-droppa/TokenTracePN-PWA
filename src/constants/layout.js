/**
 * Layout and UI size config.
 */
export const SIDEBAR_CONFIG = {
    DEFAULT_WIDTH_PX: 320,
    CODE_PANEL_DEFAULT_SIZE_PERCENT: 70,
    INSPECTOR_PANEL_DEFAULT_SIZE_PERCENT: 30,
    MIN_PANEL_SIZE_PX: 0,
};

export const CANVAS_CONFIG = {
    GRID_SIZE_PX: 40,
    ZOOM: {
        DEFAULT: 1,
        MIN: 0.2,
        MAX: 4.0,
        FACTOR: 1.1,
    },
    PANEL_BOUNDS_PX: 20000,
};